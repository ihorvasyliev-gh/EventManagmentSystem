# 📋 Краткая сводка по настройке

## ✅ Что уже готово

- ✅ Проект настроен для работы с Vite + React + TypeScript
- ✅ Создана структура компонентов и сервисов
- ✅ Настроен роутинг для Cloudflare Pages (`public/_redirects`)
- ✅ Обновлен `.gitignore` для защиты секретов
- ✅ Создана документация (README.md, DEPLOY.md, QUICKSTART.md)

## 📝 Что нужно сделать

### 1. Загрузить код на GitHub

```bash
git init
git add .
git commit -m "Initial commit: CCP Event Calendar"
git remote add origin https://github.com/ihorvasyliev-gh/CCPFlowCalendar.git
git branch -M main
git push -u origin main
```

Или следуйте инструкциям в `GITHUB_SETUP.md`

### 2. Настроить Supabase

1. Создайте проект на https://supabase.com
2. Получите URL и Anon Key (Settings → API)
3. Выполните SQL скрипт `supabase-setup.sql` в SQL Editor
4. Настройте аутентификацию

📖 **Подробные инструкции:** См. файл `SUPABASE_SETUP.md`

**Важно:** Сейчас проект использует моки. Для интеграции с Supabase:
- Установите: `npm install @supabase/supabase-js`
- Обновите сервисы (`authService.ts`, `eventService.ts`) для работы с Supabase

### 3. Настроить Cloudflare R2

1. Создайте bucket в Cloudflare R2
2. Включите Public Access
3. Получите публичный URL

### 4. Создать `.env.local` для локальной разработки

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_CLOUDFLARE_R2_PUBLIC_URL=https://pub-xxxxx.r2.dev
```

### 5. Задеплоить на Cloudflare Pages

1. Cloudflare Dashboard → Pages → Create a project
2. Подключите GitHub репозиторий
3. Настройки сборки:
   - Build command: `npm run build`
   - Build output: `dist`
4. Добавьте переменные окружения (те же, что в `.env.local`)
5. Деплой!

## 📚 Документация

- **QUICKSTART.md** - быстрый старт за 5 минут
- **README.md** - полная документация проекта
- **DEPLOY.md** - детальные инструкции по деплою
- **GITHUB_SETUP.md** - как загрузить на GitHub

## 🔗 Полезные ссылки

- GitHub репозиторий: https://github.com/ihorvasyliev-gh/CCPFlowCalendar
- Supabase: https://supabase.com
- Cloudflare Pages: https://pages.cloudflare.com
- Cloudflare R2: https://developers.cloudflare.com/r2/

## ⚠️ Важные замечания

1. **Не коммитьте `.env.local`** - он уже в `.gitignore`
2. **Моки vs Реальная БД:** Сейчас проект использует моки. Для продакшена нужно интегрировать Supabase
3. **R2 Upload:** Функция загрузки постеров в R2 сейчас симулируется. Нужно будет создать Supabase Edge Function для получения presigned URLs

## 🆘 Нужна помощь?

1. Проверьте консоль браузера (F12) на ошибки
2. Проверьте логи сборки в Cloudflare Pages
3. Убедитесь, что все переменные окружения установлены
4. Проверьте документацию в соответствующих `.md` файлах

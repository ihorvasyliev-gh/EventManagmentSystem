# Полная документация CCP Event Calendar

## 📋 Содержание

1. [Обзор приложения](#обзор-приложения)
2. [Технологический стек](#технологический-стек)
3. [Структура проекта](#структура-проекта)
4. [Описание файлов и компонентов](#описание-файлов-и-компонентов)
5. [Архитектура и принципы работы](#архитектура-и-принципы-работы)
6. [Схема базы данных](#схема-базы-данных)
7. [Основные функции](#основные-функции)
8. [Настройка и развертывание](#настройка-и-развертывание)

---

## 🎯 Обзор приложения

**CCP Event Calendar** — это веб-приложение для управления календарем событий организации Cork City Partnership. Приложение позволяет сотрудникам и администраторам создавать, просматривать, редактировать события, управлять RSVP, оставлять комментарии и экспортировать данные.

### Основные возможности:
- ✅ Аутентификация пользователей (вход/регистрация)
- 📅 Календарное представление событий (сетка и список)
- ➕ Создание и редактирование событий
- 🔄 Повторяющиеся события (recurrence)
- 📎 Вложения к событиям (постеры, документы)
- 💬 Комментарии к событиям
- 📝 История изменений событий
- ✅ RSVP (записи на события)
- 🔍 Поиск и фильтрация событий
- 📤 Экспорт событий
- 🌙 Темная/светлая тема
- 🔔 Уведомления

---

## 🛠 Технологический стек

### Frontend
- **React 19.2.3** — библиотека для создания пользовательского интерфейса
- **TypeScript 5.8.2** — типизированный JavaScript
- **Vite 6.2.0** — сборщик и dev-сервер
- **Tailwind CSS** — утилитарный CSS-фреймворк (через классы)
- **Lucide React** — иконки

### Backend & Database
- **Supabase** — Backend-as-a-Service:
  - PostgreSQL база данных
  - Supabase Auth для аутентификации
  - REST API через Supabase Client
- **Cloudflare R2** — хранилище файлов (постеры, вложения)

### Инструменты разработки
- **Vite** — сборка и разработка
- **TypeScript** — типизация
- **Git** — контроль версий

---

## 📁 Структура проекта

```
ccp-event-calendar/
├── components/              # React компоненты UI
│   ├── CalendarView.tsx     # Календарное представление (сетка/список)
│   ├── EventModal.tsx       # Модальное окно создания/редактирования события
│   ├── EventFilters.tsx     # Компонент фильтров событий
│   ├── EventComments.tsx    # Компонент комментариев
│   ├── EventHistory.tsx     # История изменений события
│   ├── ExportModal.tsx      # Модальное окно экспорта
│   ├── Navbar.tsx           # Навигационная панель
│   ├── NotificationCenter.tsx # Центр уведомлений
│   ├── SearchBar.tsx        # Поисковая строка
│   ├── SkeletonLoader.tsx   # Загрузочные скелетоны
│   └── ErrorBoundary.tsx    # Обработка ошибок React
│
├── contexts/                # React Context провайдеры
│   ├── ThemeContext.tsx     # Управление темой (светлая/темная)
│   └── ToastContext.tsx     # Система уведомлений (toast)
│
├── pages/                   # Страницы приложения
│   └── LoginPage.tsx        # Страница входа/регистрации
│
├── services/                # Сервисы для работы с API
│   ├── authService.ts       # Аутентификация (login, signup, logout)
│   ├── eventService.ts     # CRUD операции с событиями
│   ├── rsvpService.ts       # Управление RSVP (записи на события)
│   └── notificationService.ts # Управление уведомлениями
│
├── utils/                   # Вспомогательные утилиты
│   ├── date.ts             # Функции работы с датами
│   ├── filterEvents.ts     # Фильтрация событий
│   ├── recurrence.ts       # Генерация повторяющихся событий
│   ├── validation.ts       # Валидация данных
│   ├── conflictDetection.ts # Обнаружение конфликтов событий
│   └── export.ts           # Экспорт данных (CSV, JSON)
│
├── lib/                     # Библиотеки и конфигурация
│   └── supabase.ts         # Инициализация Supabase клиента
│
├── public/                  # Статические файлы
│   └── _headers            # Cloudflare Pages headers
│
├── App.tsx                  # Главный компонент приложения
├── index.tsx                # Точка входа (ReactDOM.render)
├── index.html              # HTML шаблон
├── types.ts                # TypeScript типы и интерфейсы
├── vite.config.ts          # Конфигурация Vite
├── tsconfig.json           # Конфигурация TypeScript
├── package.json            # Зависимости и скрипты
│
├── supabase-setup.sql      # SQL скрипт для настройки БД
├── README.md               # Основная документация
├── QUICKSTART.md           # Быстрый старт
├── SUPABASE_SETUP.md       # Инструкции по настройке Supabase
├── CREATE_ADMIN_USER.md    # Создание администратора
└── DEPLOY.md               # Инструкции по деплою
```

---

## 📄 Описание файлов и компонентов

### 🎨 Компоненты (`components/`)

#### `CalendarView.tsx`
**Назначение:** Отображает календарь событий в двух режимах: сетка (grid) и список (list).

**Основные функции:**
- Навигация по месяцам (предыдущий/следующий/сегодня)
- Отображение событий по дням
- Цветовая индикация категорий событий
- Переключение между режимами просмотра
- Клик по событию открывает модальное окно

**Ключевые состояния:**
- `currentDate` — текущий месяц/год
- `viewMode` — режим просмотра ('grid' | 'list')

**Использует:**
- `utils/date.ts` — функции работы с датами
- `types.ts` — типы Event, ViewMode

---

#### `EventModal.tsx`
**Назначение:** Модальное окно для создания и редактирования событий.

**Основные функции:**
- Форма создания/редактирования события
- Загрузка постеров и вложений
- Настройка повторяющихся событий (recurrence)
- Включение/выключение RSVP
- Управление статусом (draft/published)
- Просмотр комментариев и истории изменений
- Удаление события (только для создателя или админа)

**Права доступа:**
- Все пользователи могут создавать события
- Только создатель или админ могут редактировать/удалять

**Использует:**
- `services/eventService.ts` — сохранение/обновление событий
- `components/EventComments.tsx` — комментарии
- `components/EventHistory.tsx` — история

---

#### `EventFilters.tsx`
**Назначение:** Компонент фильтрации событий.

**Фильтры:**
- Категория (meeting, workshop, social, training, community, celebration, other)
- Статус (draft, published)
- Локация
- Создатель
- Теги

**Использует:**
- `types.ts` — EventFilters, EventCategory

---

#### `EventComments.tsx`
**Назначение:** Отображение и добавление комментариев к событию.

**Функции:**
- Просмотр всех комментариев
- Добавление нового комментария
- Отображение автора и времени комментария

**Использует:**
- `services/eventService.ts` — `addComment()`

---

#### `EventHistory.tsx`
**Назначение:** Отображение истории изменений события.

**Отображает:**
- Кто и когда создал/изменил событие
- Какие поля были изменены (старое → новое значение)
- Изменения статуса

**Использует:**
- `types.ts` — EventHistoryEntry

---

#### `ExportModal.tsx`
**Назначение:** Модальное окно экспорта событий.

**Форматы экспорта:**
- CSV
- JSON
- iCal (планируется)

**Использует:**
- `utils/export.ts` — функции экспорта

---

#### `Navbar.tsx`
**Назначение:** Навигационная панель вверху страницы.

**Элементы:**
- Логотип/название приложения
- Информация о пользователе
- Кнопка "Добавить событие"
- Кнопка "Экспорт"
- Кнопка выхода
- Переключатель темы

---

#### `SearchBar.tsx`
**Назначение:** Поисковая строка для событий.

**Поиск по:**
- Названию события
- Описанию
- Локации
- Тегам

---

#### `NotificationCenter.tsx`
**Назначение:** Центр уведомлений.

**Типы уведомлений:**
- Напоминания о событиях
- Создание/изменение/отмена события
- Обновления RSVP

**Использует:**
- `services/notificationService.ts`

---

#### `SkeletonLoader.tsx`
**Назначение:** Загрузочные скелетоны для улучшения UX.

**Компоненты:**
- `CalendarDaySkeleton` — скелетон дня календаря

---

#### `ErrorBoundary.tsx`
**Назначение:** Обработка ошибок React (Error Boundary).

**Функции:**
- Перехват ошибок рендеринга
- Отображение дружелюбного сообщения об ошибке
- Логирование ошибок

---

### 🎭 Контексты (`contexts/`)

#### `ThemeContext.tsx`
**Назначение:** Управление темой приложения (светлая/темная).

**Функции:**
- Сохранение темы в localStorage
- Применение класса `dark` к корневому элементу
- Переключение темы

**API:**
- `useTheme()` — хук для доступа к теме
- `toggleTheme()` — переключение темы

---

#### `ToastContext.tsx`
**Назначение:** Система уведомлений (toast notifications).

**Типы уведомлений:**
- `success` — успешная операция
- `error` — ошибка
- `warning` — предупреждение
- `info` — информация

**API:**
- `useToast()` — хук для доступа
- `showToast(message, type, duration)` — показать уведомление
- `removeToast(id)` — удалить уведомление

**Особенности:**
- Автоматическое скрытие через указанное время
- Анимация появления/исчезновения
- Отображение в правом верхнем углу

---

### 📄 Страницы (`pages/`)

#### `LoginPage.tsx`
**Назначение:** Страница входа и регистрации.

**Функции:**
- Вход существующего пользователя
- Регистрация нового пользователя
- Валидация полей
- Обработка ошибок аутентификации
- Переключение между режимами входа/регистрации

**Использует:**
- `services/authService.ts` — `login()`, `signUp()`

**Особенности:**
- Защита от множественных запросов
- Обработка таймаутов
- Отмена запросов при размонтировании компонента

---

### 🔧 Сервисы (`services/`)

#### `authService.ts`
**Назначение:** Сервис аутентификации пользователей.

**Функции:**
- `signUp(email, password, fullName, role)` — регистрация
- `login(email, password)` — вход
- `logout()` — выход
- `getCurrentUser()` — получение текущего пользователя
- `getSession()` — получение текущей сессии

**Особенности:**
- Таймауты для запросов (10 секунд)
- Обработка ошибок с понятными сообщениями
- Маппинг данных из Supabase в формат приложения
- Автоматическое создание профиля пользователя в `public.users`

**Использует:**
- `lib/supabase.ts` — Supabase клиент
- `types.ts` — User, UserRole

---

#### `eventService.ts`
**Назначение:** CRUD операции с событиями.

**Функции:**
- `getEvents()` — получение всех событий
- `createEvent(eventData, userId, userName)` — создание события
- `updateEvent(id, eventData, userId, userName)` — обновление события
- `deleteEvent(id)` — удаление события
- `addComment(eventId, userId, userName, content)` — добавление комментария
- `uploadPosterToR2(file)` — загрузка постера (симуляция)
- `uploadAttachment(file)` — загрузка вложения (симуляция)

**Особенности:**
- Загрузка связанных данных (attachments, comments, history, RSVPs)
- Маппинг между snake_case (БД) и camelCase (TypeScript)
- Автоматическое создание записей в истории изменений
- Обработка повторяющихся событий (recurrence)

**Использует:**
- `lib/supabase.ts` — Supabase клиент
- `types.ts` — Event, Attachment, EventComment, EventHistoryEntry

---

#### `rsvpService.ts`
**Назначение:** Управление записями на события (RSVP).

**Функции:**
- `rsvpToEvent(eventId, userId)` — записаться на событие
- `cancelRsvp(eventId, userId)` — отменить запись
- `getEventAttendees(eventId)` — получить список участников
- `hasUserRsvped(eventId, userId)` — проверить, записан ли пользователь

**Примечание:** В текущей версии используется мок-реализация. В продакшене должна быть интеграция с Supabase таблицей `rsvps`.

---

#### `notificationService.ts`
**Назначение:** Управление уведомлениями.

**Функции:**
- `requestNotificationPermission()` — запрос разрешения на уведомления
- `showBrowserNotification(title, options)` — показать браузерное уведомление
- `createNotification(notification)` — создать уведомление
- `getNotifications(userId, unreadOnly)` — получить уведомления
- `markNotificationAsRead(id)` — отметить как прочитанное
- `markAllNotificationsAsRead()` — отметить все как прочитанные
- `deleteNotification(id)` — удалить уведомление
- `scheduleEventReminder(event, reminderMinutes)` — запланировать напоминание

**Примечание:** В текущей версии используется мок-реализация. В продакшене должна быть интеграция с Supabase.

---

### 🛠 Утилиты (`utils/`)

#### `date.ts`
**Назначение:** Функции работы с датами.

**Функции:**
- `getDaysInMonth(date)` — количество дней в месяце
- `getFirstDayOfMonth(date)` — день недели первого дня месяца
- `isSameDay(date1, date2)` — проверка, один ли день
- `addMonths(date, months)` — добавить месяцы к дате
- `formatDate(date)` — форматирование даты
- `formatTime(date)` — форматирование времени

---

#### `filterEvents.ts`
**Назначение:** Фильтрация массива событий по различным критериям.

**Функции:**
- `filterEvents(events, filters)` — фильтрация событий

**Поддерживаемые фильтры:**
- Поиск по тексту (название, описание, локация, теги)
- Категория
- Статус
- Диапазон дат
- Локация
- Создатель
- Теги

**Использует:**
- `types.ts` — EventFilters

---

#### `recurrence.ts`
**Назначение:** Работа с повторяющимися событиями.

**Функции:**
- `generateRecurringEvents(baseEvent, rule, count)` — генерация повторяющихся событий
- `formatRecurrenceRule(rule)` — форматирование правила повторения

**Типы повторений:**
- `daily` — ежедневно
- `weekly` — еженедельно
- `monthly` — ежемесячно
- `yearly` — ежегодно
- `custom` — пользовательский (с интервалом)

**Параметры:**
- `interval` — интервал повторения
- `endDate` — дата окончания
- `occurrences` — количество повторений
- `daysOfWeek` — дни недели (для weekly)

**Использует:**
- `types.ts` — RecurrenceRule

---

#### `validation.ts`
**Назначение:** Валидация данных форм.

**Функции:**
- Валидация email
- Валидация пароля
- Валидация данных события
- Проверка обязательных полей

---

#### `conflictDetection.ts`
**Назначение:** Обнаружение конфликтов событий (пересечение по времени).

**Функции:**
- Проверка пересечения дат/времени событий
- Предупреждение о конфликтах

---

#### `export.ts`
**Назначение:** Экспорт событий в различные форматы.

**Функции:**
- Экспорт в CSV
- Экспорт в JSON
- Экспорт в iCal (планируется)

---

### 📚 Библиотеки (`lib/`)

#### `supabase.ts`
**Назначение:** Инициализация Supabase клиента.

**Функции:**
- Создание клиента Supabase
- Проверка наличия переменных окружения
- Настройка аутентификации (autoRefreshToken, persistSession)

**Переменные окружения:**
- `VITE_SUPABASE_URL` — URL проекта Supabase
- `VITE_SUPABASE_ANON_KEY` — анонимный ключ API

**Использует:**
- `@supabase/supabase-js` — официальный клиент Supabase

---

### 🎯 Главные файлы

#### `App.tsx`
**Назначение:** Главный компонент приложения.

**Структура:**
```tsx
App (ErrorBoundary)
  └── ThemeProvider
      └── ToastProvider
          └── AppContent
              ├── LoginPage (если не авторизован)
              └── Main App (если авторизован)
                  ├── Navbar
                  ├── SearchBar + EventFilters
                  ├── CalendarView
                  ├── EventModal
                  └── ExportModal
```

**Основные состояния:**
- `user` — текущий пользователь
- `events` — список событий
- `loadingEvents` — загрузка событий
- `searchQuery` — поисковый запрос
- `filters` — фильтры событий
- `isModalOpen` — открыто ли модальное окно
- `selectedEvent` — выбранное событие

**Основные эффекты:**
- Проверка сессии при монтировании
- Подписка на изменения аутентификации
- Загрузка событий при входе пользователя

**Обработчики:**
- `handleLogin` — обработка входа
- `handleLogout` — обработка выхода
- `handleEventClick` — клик по событию
- `handleCreateClick` — создание нового события
- `handleSaveEvent` — сохранение события
- `handleUpdateEvent` — обновление события

**Использует:**
- Все компоненты из `components/`
- Все сервисы из `services/`
- Контексты из `contexts/`
- Утилиты из `utils/`

---

#### `index.tsx`
**Назначение:** Точка входа приложения.

**Функции:**
- Поиск корневого элемента DOM
- Создание React root
- Рендеринг App компонента в StrictMode

---

#### `types.ts`
**Назначение:** TypeScript типы и интерфейсы.

**Основные типы:**
- `User` — пользователь
- `UserRole` — роли пользователей (staff, admin)
- `Event` — событие
- `EventStatus` — статусы события (draft, published)
- `EventCategory` — категории событий
- `RecurrenceRule` — правило повторения
- `Attachment` — вложение
- `EventComment` — комментарий
- `EventHistoryEntry` — запись истории
- `EventFilters` — фильтры событий
- `ViewMode` — режимы просмотра

---

#### `vite.config.ts`
**Назначение:** Конфигурация Vite.

**Настройки:**
- Порт dev-сервера: 3000
- Плагин React
- Настройки сборки (outDir: 'dist')
- Алиасы путей (@ → корень проекта)
- Загрузка переменных окружения

---

#### `package.json`
**Назначение:** Зависимости и скрипты проекта.

**Зависимости:**
- `react`, `react-dom` — React
- `@supabase/supabase-js` — Supabase клиент
- `lucide-react` — иконки

**Dev-зависимости:**
- `typescript` — TypeScript
- `vite` — сборщик
- `@vitejs/plugin-react` — плагин React для Vite

**Скрипты:**
- `npm run dev` — запуск dev-сервера
- `npm run build` — сборка для продакшена
- `npm run preview` — предпросмотр production сборки

---

## 🏗 Архитектура и принципы работы

### Общая архитектура

Приложение построено по принципу **SPA (Single Page Application)** с использованием React и Supabase в качестве Backend-as-a-Service.

```
┌─────────────────────────────────────────┐
│         React Application               │
│  (Frontend - Vite + React + TS)        │
├─────────────────────────────────────────┤
│  Components → Services → Supabase API  │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         Supabase Backend                │
│  - PostgreSQL Database                  │
│  - Authentication                       │
│  - REST API                             │
│  - Row Level Security (RLS)             │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│      Cloudflare R2 Storage              │
│  (Posters, Attachments)                 │
└─────────────────────────────────────────┘
```

### Поток данных

#### 1. Аутентификация
```
User → LoginPage → authService.login() 
  → Supabase Auth → getCurrentUser() 
  → App.setState(user) → Main App
```

#### 2. Загрузка событий
```
App (useEffect) → eventService.getEvents() 
  → Supabase API → mapSupabaseEventToEvent() 
  → App.setState(events) → CalendarView
```

#### 3. Создание события
```
User → EventModal → eventService.createEvent() 
  → Supabase API (insert) 
  → Trigger (event_history) 
  → App.setState(events) → CalendarView (обновление)
```

#### 4. Обновление события
```
User → EventModal → eventService.updateEvent() 
  → Supabase API (update) 
  → event_history.insert() (изменения) 
  → App.setState(events) → CalendarView (обновление)
```

### Управление состоянием

Приложение использует **локальное состояние React** (useState) и **Context API** для глобального состояния:

- **Локальное состояние:**
  - Состояние компонентов (модальные окна, формы)
  - Фильтры и поиск
  - Выбранное событие

- **Context API:**
  - `ThemeContext` — тема приложения
  - `ToastContext` — уведомления

- **Supabase как источник истины:**
  - Пользователи
  - События
  - Комментарии
  - История изменений
  - RSVP

### Обработка ошибок

1. **Error Boundary** (`ErrorBoundary.tsx`) — перехват ошибок рендеринга
2. **Try-catch** в сервисах — обработка ошибок API
3. **Toast уведомления** — показ ошибок пользователю
4. **Таймауты** — защита от зависших запросов

### Безопасность

1. **Row Level Security (RLS)** в Supabase — контроль доступа на уровне БД
2. **Проверка прав** в компонентах (только создатель/админ может редактировать)
3. **Валидация данных** на клиенте и сервере
4. **Аутентификация** через Supabase Auth

---

## 🗄 Схема базы данных

### Таблицы

#### `public.users`
Расширенная информация о пользователях (дополняет `auth.users`).

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | PK, ссылка на `auth.users.id` |
| `email` | TEXT | Email (уникальный) |
| `full_name` | TEXT | Полное имя |
| `role` | TEXT | Роль: 'staff' или 'admin' |
| `created_at` | TIMESTAMP | Дата создания |
| `updated_at` | TIMESTAMP | Дата обновления |

---

#### `public.events`
Основная таблица событий.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | PK |
| `title` | TEXT | Название события |
| `description` | TEXT | Описание |
| `date` | TIMESTAMP | Дата и время события |
| `location` | TEXT | Локация |
| `poster_url` | TEXT | URL постера |
| `status` | TEXT | Статус: 'draft', 'published' |
| `category` | TEXT | Категория |
| `tags` | TEXT[] | Массив тегов |
| `recurrence_type` | TEXT | Тип повторения |
| `recurrence_interval` | INTEGER | Интервал повторения |
| `recurrence_end_date` | TIMESTAMP | Дата окончания повторения |
| `recurrence_occurrences` | INTEGER | Количество повторений |
| `recurrence_days_of_week` | INTEGER[] | Дни недели для weekly |
| `rsvp_enabled` | BOOLEAN | Включен ли RSVP |
| `max_attendees` | INTEGER | Максимум участников |
| `creator_id` | UUID | FK → `users.id` |
| `created_at` | TIMESTAMP | Дата создания |
| `updated_at` | TIMESTAMP | Дата обновления |

---

#### `public.event_attachments`
Вложения к событиям (постеры, документы).

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | PK |
| `event_id` | UUID | FK → `events.id` |
| `name` | TEXT | Имя файла |
| `url` | TEXT | URL файла (R2) |
| `type` | TEXT | Тип: 'image', 'pdf', 'document', 'other' |
| `size` | BIGINT | Размер файла (байты) |
| `uploaded_at` | TIMESTAMP | Дата загрузки |
| `uploaded_by` | UUID | FK → `users.id` |

---

#### `public.event_comments`
Комментарии к событиям.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | PK |
| `event_id` | UUID | FK → `events.id` |
| `user_id` | UUID | FK → `users.id` |
| `user_name` | TEXT | Имя пользователя (denormalized) |
| `content` | TEXT | Текст комментария |
| `created_at` | TIMESTAMP | Дата создания |

---

#### `public.event_history`
История изменений событий.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | PK |
| `event_id` | UUID | FK → `events.id` |
| `user_id` | UUID | FK → `users.id` |
| `user_name` | TEXT | Имя пользователя (denormalized) |
| `action` | TEXT | Действие: 'created', 'updated', 'deleted', 'status_changed' |
| `changes` | JSONB | Изменения в формате {field: {old, new}} |
| `timestamp` | TIMESTAMP | Время изменения |

---

#### `public.rsvps`
Записи на события (RSVP).

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | PK |
| `event_id` | UUID | FK → `events.id` |
| `user_id` | UUID | FK → `users.id` |
| `user_name` | TEXT | Имя пользователя (denormalized) |
| `status` | TEXT | Статус: 'going', 'maybe', 'not_going' |
| `created_at` | TIMESTAMP | Дата создания |
| `updated_at` | TIMESTAMP | Дата обновления |
| UNIQUE(`event_id`, `user_id`) | | Один RSVP на пользователя на событие |

---

### Триггеры и функции

#### Триггер `create_user_profile`
Автоматически создает запись в `public.users` при регистрации в `auth.users`.

#### Триггер `update_updated_at`
Автоматически обновляет `updated_at` при изменении записи.

#### Триггер `log_event_history`
Автоматически создает запись в `event_history` при создании/изменении события.

---

### Row Level Security (RLS)

Политики безопасности на уровне строк:

- **Пользователи** могут читать все события, создавать свои, редактировать/удалять только свои (или админ может все)
- **Комментарии** — все могут читать, создавать свои, удалять только свои
- **RSVP** — все могут читать, создавать/обновлять свои

---

## ⚙️ Основные функции

### 1. Аутентификация и авторизация

- **Регистрация:** Создание нового аккаунта (по умолчанию роль 'staff')
- **Вход:** Авторизация существующего пользователя
- **Выход:** Завершение сессии
- **Роли:** 'staff' (сотрудник) и 'admin' (администратор)
- **Сессии:** Автоматическое сохранение и обновление токенов

### 2. Управление событиями

- **Создание:** Форма с полями (название, описание, дата, локация, категория, теги)
- **Редактирование:** Изменение существующих событий (только создатель/админ)
- **Удаление:** Удаление событий (только создатель/админ)
- **Статусы:** draft (черновик), published (опубликовано). Черновики видят только админы.

### 3. Повторяющиеся события

- **Типы:** daily, weekly, monthly, yearly, custom
- **Параметры:** интервал, дата окончания, количество повторений, дни недели
- **Генерация:** Автоматическая генерация повторяющихся событий

### 4. Вложения

- **Типы:** изображения, PDF, документы
- **Хранение:** Cloudflare R2 (в продакшене)
- **Загрузка:** Через форму в EventModal

### 5. Комментарии

- **Добавление:** Любой авторизованный пользователь может комментировать
- **Отображение:** Хронологический список с автором и временем
- **Хранение:** Таблица `event_comments`

### 6. История изменений

- **Автоматическое логирование:** Все изменения сохраняются
- **Детали:** Кто, когда, что изменил (старое → новое значение)
- **Отображение:** В EventModal вкладка "History"

### 7. RSVP

- **Запись на событие:** Пользователи могут записаться
- **Статусы:** going (иду), maybe (возможно), not_going (не иду)
- **Ограничения:** Максимальное количество участников (если указано)

### 8. Поиск и фильтрация

- **Поиск:** По названию, описанию, локации, тегам
- **Фильтры:** Категория, статус, локация, создатель, теги, диапазон дат
- **Комбинирование:** Поиск + фильтры работают вместе

### 9. Экспорт

- **Форматы:** CSV, JSON
- **Фильтрация:** Экспортируются только отфильтрованные события
- **Поля:** Все основные поля события

### 10. Темы

- **Светлая тема:** По умолчанию
- **Темная тема:** Переключение через Navbar
- **Сохранение:** В localStorage

### 11. Уведомления

- **Типы:** Напоминания, создание/изменение событий, RSVP
- **Браузерные уведомления:** С разрешением пользователя
- **Центр уведомлений:** Компонент NotificationCenter

---

## 🚀 Настройка и развертывание

### Локальная разработка

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd ccp-event-calendar
```

2. **Установка зависимостей:**
```bash
npm install
```

3. **Настройка переменных окружения:**
Создайте файл `.env.local`:
```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_CLOUDFLARE_R2_PUBLIC_URL=https://your-r2-bucket.r2.dev
```

4. **Настройка Supabase:**
- Создайте проект на [supabase.com](https://supabase.com)
- Выполните SQL скрипт `supabase-setup.sql` в SQL Editor
- Настройте Authentication (отключите подтверждение email для быстрого входа)
- Скопируйте URL и Anon Key в `.env.local`

5. **Запуск dev-сервера:**
```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

---

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

---

### Деплой на Cloudflare Pages

#### Автоматический деплой через GitHub

1. Подключите репозиторий в Cloudflare Dashboard → Pages
2. Настройки сборки:
   - Build command: `npm run build`
   - Build output directory: `dist`
3. Добавьте переменные окружения:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
   - `VITE_CLOUDFLARE_R2_PUBLIC_URL`
4. Cloudflare автоматически задеплоит при каждом push в main

#### Ручной деплой через Wrangler

```bash
npm install -g wrangler
wrangler login
npm run build
wrangler pages deploy dist --project-name=ccp-event-calendar
```

---

### Настройка Cloudflare R2

1. Создайте bucket в Cloudflare Dashboard → R2
2. Включите Public Access
3. Настройте Custom Domain (опционально)
4. Скопируйте публичный URL в `.env.local`

---

### Создание администратора

См. файл `CREATE_ADMIN_USER.md` для инструкций по созданию администратора через SQL или Supabase Dashboard.

---

## 📝 Примечания

### Текущие ограничения

1. **RSVP Service** — использует мок-реализацию, нужна интеграция с Supabase
2. **Notification Service** — использует мок-реализацию, нужна интеграция с Supabase
3. **Загрузка файлов** — симуляция, нужна реальная интеграция с Cloudflare R2
4. **iCal экспорт** — планируется, пока только CSV/JSON

### Будущие улучшения

- [ ] Реальная интеграция RSVP с Supabase
- [ ] Реальная интеграция уведомлений с Supabase
- [ ] Реальная загрузка файлов в Cloudflare R2
- [ ] Экспорт в iCal формат
- [ ] Push-уведомления
- [ ] Email-уведомления
- [ ] Мобильная версия
- [ ] Офлайн-режим (PWA)

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте переменные окружения
2. Убедитесь, что Supabase проект настроен корректно
3. Проверьте права доступа к R2 bucket
4. Проверьте консоль браузера на наличие ошибок
5. Проверьте логи Supabase Dashboard

---

**Версия документации:** 1.0  
**Дата обновления:** 2024  
**Автор:** CCP Development Team

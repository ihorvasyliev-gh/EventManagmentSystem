# 📤 Загрузка проекта на GitHub

## Первоначальная настройка

Если вы еще не инициализировали git репозиторий:

```bash
# Инициализация git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: CCP Event Calendar"

# Подключение к удаленному репозиторию
git remote add origin https://github.com/ihorvasyliev-gh/CCPFlowCalendar.git

# Переименование ветки в main (если нужно)
git branch -M main

# Отправка на GitHub
git push -u origin main
```

## Если репозиторий уже существует

```bash
# Проверка текущего статуса
git status

# Добавление изменений
git add .

# Коммит
git commit -m "Описание ваших изменений"

# Отправка на GitHub
git push origin main
```

## Важные замечания

⚠️ **НЕ коммитьте файлы с секретами:**
- `.env.local` - уже в `.gitignore`
- Файлы с паролями и ключами

✅ **Что будет загружено:**
- Весь исходный код
- Конфигурационные файлы
- Документация (README, DEPLOY.md и т.д.)
- `env.example.txt` (пример переменных окружения)

## Проверка перед коммитом

```bash
# Посмотреть, что будет закоммичено
git status

# Посмотреть изменения
git diff
```

## Если нужно обновить .gitignore

```bash
# Если вы уже закоммитили файлы, которые нужно игнорировать:
git rm --cached .env.local
git commit -m "Remove .env.local from tracking"
```

## Полезные команды

```bash
# Просмотр истории коммитов
git log

# Отмена последнего коммита (но сохранить изменения)
git reset --soft HEAD~1

# Просмотр удаленных репозиториев
git remote -v
```
